{% extends 'base.html.twig' %}

{% block title %}Valider ma commande : La boutique E-Commerce{% endblock %}

{% block content %}
    <h2>Je passe ma commande</h2>
    <p>Choisissez vos prédérences avant de passer votre commande sur La boutique E-Commerce.</p>
    <hr>
    <div class="row">
        <div class="col-md-6">
            {% set formHtml %}
                {{ form(form) }}
            {% endset %}

            {{ formHtml|replace({'[br]' : '</br>'})|raw }}

        </div>
        <div class="col-md-6">
            <b>Récap de ma commande</b></br>
            {# {{ dump(cart) }} #}
            {% for item in cart %}
                <div class="row">
                    <div class="col-2">
                        <img src="/uploads/{{ item.product.illustration }}" alt="{{ item.product.name }}" height="75px"></br>
                    </div>
                    <div class="col-8 my-auto">
                        {{ item.product.name }}</br>
                        <small>{{ item.product.subtitle }}</small>
                    </div>
                    <div class="col-2 my-auto">
                        x {{ item.quantity }}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
{% block widget %}
{% endblock %}